// Firebase Configuration Template
// INSTRUCTIONS:
// 1. Copy this file and rename it to: firebase-config.js
// 2. Replace the placeholder values with your actual Firebase credentials
// 3. NEVER commit firebase-config.js to a public repository!

const firebaseConfig = {
    apiKey: "YOUR_FIREBASE_API_KEY_HERE",
    authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
    projectId: "YOUR_PROJECT_ID",
    storageBucket: "YOUR_PROJECT_ID.firebasestorage.app",
    messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
    appId: "YOUR_APP_ID"
};

// SECURITY NOTES:
// - Keep firebase-config.js in .gitignore (already configured)
// - Restrict your API key in Firebase Console to your domain only
// - Never share your actual credentials publicly
// - If credentials are exposed, rotate them immediately in Firebase Console
